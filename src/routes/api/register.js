/** @type {import('@sveltejs/kit').RequestHandler} */
export const post = async () => {

  // General zod validation (email unique include?)
  // Is email unique?
  // hash password
  // save user
  // login user using cookie
  
}